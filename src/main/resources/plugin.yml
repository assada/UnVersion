name: Unversion # the plugins name as it should appear in the plugin list /pl
version: 1.0.1 # the plugin's version
author: Assada
main: Unversion.Main
api-version: 1.18 # the version of the API you want to use, required starting with 1.13
depend:
  - ProtocolLib